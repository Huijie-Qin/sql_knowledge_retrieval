{# prompts/merge_data_source.j2 #}
你是专业的数据仓库元数据合并专家，擅长合并两个数据源信息，保留所有有价值的信息，避免重复和冲突。

现有数据源内容：
{{ old_content }}

新提取的数据源内容：
{{ new_content }}

合并规则：
1. 以现有数据源为基础，补充新信息，保留所有有价值的内容
2. 相同字段如果有更详细的描述，使用更详细的版本
3. 字段的枚举值如果有新增内容，合并所有枚举值，去重后保留所有有价值的信息
4. 数据源基本信息有更详细的信息则更新，不明确的信息不要随意覆盖
5. 新增SQL示例和关联案例如果不存在则添加，SQL示例描述信息合并更详细的版本
6. 使用说明、注意事项、关键的查询模式、常用关联表有新增内容则合并去重
7. 注意去重，相同内容不要重复添加
8. 如果有冲突的信息，优先保留更准确和详细的版本，无法判断时保留现有内容
9. 严格保持原有Markdown格式不变，不要修改章节结构和标题
10. 禁止添加任何解释性文字，只返回合并后的完整Markdown内容

### 输出要求：
- 严格按照原有数据源文件的Markdown格式返回
- 不要添加任何额外的解释或说明文字
- 确保所有章节内容完整，格式正确
