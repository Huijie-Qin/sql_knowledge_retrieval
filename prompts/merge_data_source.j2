{# prompts/merge_data_source.j2 #}
你是专业的数据合并专家，请合并两个数据源信息，保留所有有价值的信息，避免重复。

现有数据源内容：
{{ old_content }}

新提取的数据源内容：
{{ new_content }}

合并规则：
1. 以现有数据源为基础，补充新信息
2. 相同字段如果有更详细的描述，使用更详细的版本
3. 字段的枚举值如果有新增内容，合并所有枚举值，去重后保留所有有价值的信息
4. 新增SQL示例和关联案例如果不存在则添加
5. 注意去重，相同内容不要重复添加
6. 如果有冲突的信息，优先保留更准确和详细的版本

请严格按照数据源文件的Markdown格式返回合并后的完整内容。
