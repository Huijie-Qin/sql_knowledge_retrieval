我需要你解析Markdown格式的文档内容，抽取以下关键信息，以JSON格式返回。

### 抽取章节：基本信息 + 表结构（章节1+2）

### 输出JSON结构：
```json
{
  "basic_info": {
    "document_name": "文档名称",
    "business_scenario": "业务场景描述",
    "purpose": "文档用途说明",
    "author": "作者/负责人",
    "create_time": "创建时间",
    "last_update_time": "最后更新时间",
    "version": "版本号"
  },
  "table_structure": [
    {
      "table_name": "表名",
      "table_description": "表描述",
      "columns": [
        {
          "column_name": "字段名",
          "column_type": "字段类型",
          "column_description": "字段描述",
          "is_primary_key": "是否主键（true/false）",
          "is_nullable": "是否可为空（true/false）"
        }
      ]
    }
  ]
}
```

### 要求：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误
3. 如果某个字段没有对应信息，填空字符串""
4. 时间格式统一为"YYYY-MM-DD"，如果信息不全则留空
5. 布尔值必须是小写的true或false，不要用字符串
6. 从Markdown表格中准确提取表结构信息

### 待解析文档内容：
{{ document_content }}
