我需要你解析Markdown格式的文档内容，抽取以下关键信息，以JSON格式返回。

### 抽取章节：基本信息 + 表结构（章节1+2）

### 输出JSON结构：
```json
{
  "business_domain": "识别所属业务域（广告/应用/音乐/公共/全域搜索/电商/其他）",
  "data_sources": [
    {
      "table_name": "完整表名（如：biads.ads_persona_daas_common_pre_traffic_insight_dm）",
      "database": "数据库名（从表名前缀提取，如biads）",
      "snowflake_layer": "雪花层（从表名和内容推断，如：ADS（应用数据层）- DAAS画像/流量洞察、DWD（明细数据层）等，没有则留空）",
      "partition_field": "分区字段（从SQL的WHERE条件中提取，如pt_d）",
      "main_usage": "主要用途（从案例文件中提取并推理得到的表的主要用途）",
      "description": "数据源描述",
      "fields": [
        {
          "name": "字段名",
          "description": "字段描述",
          "usage": "用途说明",
          "enum_values": "字段的常用枚举值及含义，格式如：枚举值1:含义1,枚举值2:含义2，没有则留空"
        }
      ]
    }
  ]
}
```

### 要求：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误
3. 如果某个字段没有对应信息，填空字符串""
4. 时间格式统一为"YYYY-MM-DD"，如果信息不全则留空
5. 布尔值必须是小写的true或false，不要用字符串
6. 从Markdown表格中准确提取表结构信息

### 待解析文档内容：
{{ content }}
