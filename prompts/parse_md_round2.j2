我需要你解析Markdown格式的文档内容，抽取以下关键信息，以JSON格式返回。

### 抽取章节：SQL示例 + 使用说明（章节3+4）

### 输出JSON结构：
```json
{
  "data_sources": [
    {
      "table_name": "表名（必须填写，用于合并匹配）",
      "sql_examples": [
        {
          "name": "示例名称",
          "description": "示例描述",
          "sql": "SQL代码内容",
          "expected_output": "预期输出说明"
        }
      ],
      "usage_instructions": "使用说明，详细描述该数据源的使用方法",
      "notes": "注意事项，描述使用该数据源时需要特别注意的问题",
      "key_query_patterns": [
        "关键查询模式1（如：按日期分区查询、按事件类型分组统计、按品类维度聚合等，每条用一句话描述）",
        "关键查询模式2"
      ]
    }
  ]
}
```

### 要求：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误
3. 如果某个字段没有对应信息，填空字符串""
4. 从Markdown代码块中提取SQL代码，保留完整格式，包括换行和缩进
5. typical_application_scenarios是数组类型，如果没有相关信息则返回空数组[]
6. 准确识别Markdown中的列表、表格等结构中的使用说明信息

### 待解析文档内容：
{{ document_content }}
