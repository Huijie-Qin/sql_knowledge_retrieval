我需要你解析Markdown格式的文档内容，抽取以下关键信息，以JSON格式返回。

### 抽取章节：数据质量 + 关联案例（章节5+6）

### 输出JSON结构：
```json
{
  "data_sources": [
    {
      "table_name": "表名（必须填写，用于合并匹配）",
      "data_quality": {
        "daily_records": "日记录数",
        "daily_users": "日覆盖用户数",
        "coverage": "数据覆盖情况",
        "timeliness": "上报及时性"
      },
      "related_cases": [
        {
          "name": "案例名称",
          "description": "案例描述",
          "business_background": "业务背景",
          "solution": "解决方案",
          "effect": "实施效果",
          "reference_link": "参考链接（如果有）"
        }
      ]
    }
  ]
}
```

### 要求：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误
3. 如果某个字段没有对应信息，填空字符串""
4. 数组类型字段如果没有相关信息则返回空数组[]
5. 从Markdown的列表、表格等结构中准确提取数据质量字段和关联案例信息
6. 关联案例需要尽可能完整抽取，包括业务背景、解决方案、实施效果等字段

### 待解析文档内容：
{{ content }}
