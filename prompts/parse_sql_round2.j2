我需要你解析SQL相关的文档内容，抽取以下关键信息，以JSON格式返回。

### 抽取章节：SQL示例 + 使用说明（章节3+4）

### 输出JSON结构：
```json
{
  "sql_examples": [
    {
      "example_name": "示例名称",
      "example_description": "示例描述",
      "sql_code": "SQL代码内容",
      "expected_output": "预期输出说明"
    }
  ],
  "usage_instructions": {
    "prerequisites": "使用前提条件",
    "execution_steps": "执行步骤说明",
    "parameter_description": "参数说明",
    "typical_application_scenarios": [
      "典型应用场景1",
      "典型应用场景2"
    ],
    "notes": "注意事项"
  }
}
```

### 要求：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误
3. 如果某个字段没有对应信息，填空字符串""
4. SQL代码需要保留完整格式，包括换行和缩进
5. typical_application_scenarios是数组类型，如果没有相关信息则返回空数组[]

### 待解析文档内容：
{{ document_content }}
