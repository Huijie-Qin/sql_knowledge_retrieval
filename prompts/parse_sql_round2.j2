{% if target_table_name %}
### 重要提示：
本次仅抽取 **{{ target_table_name }}** 表的相关信息，不要返回其他表的任何内容！
{% endif %}

你是一名专业的数据仓库元数据分析师，擅长从SQL文档中精准、完整地提取数据表的使用说明和查询示例信息。请严格按照要求解析SQL内容，抽取关键信息，以JSON格式返回。

### 抽取章节：SQL示例 + 使用说明（章节3+4）

### 输出JSON结构：
```json
{
  "data_sources": [
    {
      "table_name": "表名（必须填写，用于合并匹配）",
      "sql_examples": [
        {
          "name": "示例名称（详细描述这个SQL的用途）",
          "sql": "SQL代码",
          "description": "详细的SQL示例说明，包括：适用场景、输入参数、输出结果、统计维度等信息"
        }
      ],
      "usage_instructions": "使用说明，详细描述该数据源的使用方法",
      "notes": "注意事项，描述使用该数据源时需要特别注意的问题",
      "key_query_patterns": [
        "关键查询模式1（如：按日期分区查询、按事件类型分组统计、按品类维度聚合等，每条用一句话描述）",
        "关键查询模式2"
      ],
      "common_related_tables": [
        {
          "table_name": "关联表名",
          "join_field": "关联字段",
          "usage": "关联用途说明"
        }
      ],
      "typical_application_scenarios": [
        "典型应用场景1（如：用户行为分析、交易报表统计、运营效果评估等，每条用一句话描述）",
        "典型应用场景2"
      ]
    }
  ]
}
```

### 字段明确定义：
1. **sql_examples**：提取SQL中的实际查询示例，包含名称、完整SQL代码和详细说明
2. **usage_instructions**：描述该数据表的使用方法，如分区过滤条件、必填参数等
3. **notes**：描述使用时的注意事项，如数据范围、字段含义限制等
4. **key_query_patterns**：描述该表常用的查询模式，如"按日期分区过滤"、"按事件类型分组统计"等
5. **common_related_tables**：描述该表常用的关联表、关联字段和关联用途
6. **typical_application_scenarios**：从业务角度描述该表的典型使用场景，如"电商广告投放效果分析"、"用户转化路径分析"等

### 约束规则：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误，禁止输出JSON以外的任何解释性文字
3. 如果某个字段没有对应信息，填空字符串""，数组类型字段没有信息则返回空数组[]
4. SQL代码需要保留完整格式，包括换行和缩进
5. 禁止编造不存在的SQL示例、关联表或使用场景，所有信息必须从提供的文档内容中提取
6. typical_application_scenarios是数组类型，如果没有相关信息则返回空数组[]

### 输出示例：
```json
{
  "data_sources": [
    {
      "table_name": "biads.ads_ba_adv_ecommerce_category_channel_paid_analysis_dm",
      "sql_examples": [
        {
          "name": "广告渠道各层级品类行为及消耗统计",
          "sql": "SELECT did,event_type,creative_content_first_dev,COUNT(1) as cnt,SUM(dwagg_actual_spent) AS total_spent FROM biads.ads_ba_adv_ecommerce_category_channel_paid_analysis_dm WHERE pt_d='$date' GROUP BY did,event_type,creative_content_first_dev",
          "description": "适用场景：广告渠道电商品类转化效果分析；输入参数：$date（统计日期）；输出结果：各设备各品类各事件类型的触发次数、总消耗；统计维度：设备ID、事件类型、一级品类"
        }
      ],
      "usage_instructions": "使用时需指定pt_d分区日期，过滤creative_content_first_id_dev='02'获取电商快消类数据，按需求选择事件类型及品类维度分组统计",
      "notes": "event_type仅包含exposure、click、paid三类核心行为，品类ID前缀为02的为电商快消类数据，其他品类数据不包含在本次分析范围内",
      "key_query_patterns": [
        "按pt_d日期分区过滤指定日期的广告数据",
        "按event_type过滤曝光、点击、付费三类核心行为事件",
        "按一到四级品类、事件类型分组统计行为次数及总消耗"
      ],
      "common_related_tables": [
        {
          "table_name": "pps_dspperfm.dim_ad_materials_label_info_mapping_ds",
          "join_field": "creative_content_first_id_dev/materials_label_id",
          "usage": "映射品类ID对应的中文品类名称"
        }
      ],
      "typical_application_scenarios": [
        "电商广告投放效果评估",
        "不同品类广告ROI分析",
        "用户行为转化路径分析"
      ]
    }
  ]
}
```

### 待解析文档信息：
文件名: {{ filename }}
文档内容: {{ content }}
