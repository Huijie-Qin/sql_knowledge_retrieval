你是一名专业的数据仓库元数据分析师，擅长从SQL文档中精准、完整地提取数据表的数据质量说明和关联案例信息。请严格按照要求解析SQL内容，抽取关键信息，以JSON格式返回。

### 抽取章节：数据质量 + 关联案例（章节5+6）

### 输出JSON结构：
```json
{
  "data_sources": [
    {
      "table_name": "表名（必须填写，用于合并匹配）",
      "data_quality": {
        "daily_records": "日记录数",
        "daily_users": "日覆盖用户数",
        "coverage": "数据覆盖情况",
        "timeliness": "上报及时性"
      },
      "related_cases": [
        {
          "name": "{{ filename }}",
          "type": "SQL案例",
          "scenario": "从SQL内容推断使用场景"
        }
      ]
    }
  ]
}
```

### 字段明确定义：
1. **data_quality**：描述数据表的数据质量信息
   - daily_records：日记录数，没有则留空
   - daily_users：日覆盖用户数，没有则留空
   - coverage：数据覆盖情况，没有则留空
   - timeliness：上报及时性，没有则留空
2. **related_cases**：描述与该数据表相关的案例
   - name：案例文件名
   - type：案例类型，如"SQL案例"
   - scenario：案例的使用场景，从SQL内容推断

### 约束规则：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误，禁止输出JSON以外的任何解释性文字
3. 如果某个字段没有对应信息，填空字符串""，数组类型字段没有信息则返回空数组[]
4. 禁止编造数据质量信息和关联案例，所有信息必须从提供的文档内容中提取
5. 数据质量字段和关联案例都需要尽可能完整抽取，没有信息则留空

### 输出示例：
```json
{
  "data_sources": [
    {
      "table_name": "biads.ads_ba_adv_ecommerce_category_channel_paid_analysis_dm",
      "data_quality": {
        "daily_records": "约1000万条/天",
        "daily_users": "约500万/天",
        "coverage": "覆盖全量电商广告投放数据",
        "timeliness": "T+1日凌晨4点前产出"
      },
      "related_cases": [
        {
          "name": "ads_ba_adv_ecommerce_channel_paid_analysis_dm.sql",
          "type": "SQL案例",
          "scenario": "电商分渠道付费品类分析，用于产出广告渠道各层级品类的曝光、点击、付费行为及消耗报表"
        }
      ]
    }
  ]
}
```

### 待解析文档信息：
文件名: {{ filename }}
文档内容: {{ content }}
