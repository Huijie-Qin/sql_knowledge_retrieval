我需要你解析SQL相关的文档内容，抽取以下关键信息，以JSON格式返回。

### 抽取章节：数据质量 + 关联案例（章节5+6）

### 输出JSON结构：
```json
{
  "data_sources": [
    {
      "table_name": "表名（必须填写，用于合并匹配）",
      "data_quality": {
        "data_source": "数据来源说明",
        "accuracy": "数据准确性说明",
        "completeness": "数据完整性说明",
        "timeliness": "数据时效性说明",
        "consistency": "数据一致性说明",
        "quality_check_rules": [
          {
            "rule_name": "规则名称",
            "rule_description": "规则描述",
            "check_method": "检查方法"
          }
        ]
      },
      "related_cases": [
        {
          "name": "案例名称",
          "description": "案例描述",
          "business_background": "业务背景",
          "solution": "解决方案",
          "effect": "实施效果",
          "reference_link": "参考链接（如果有）"
        }
      ]
    }
  ]
}
```

### 要求：
1. 严格按照上述JSON结构输出，不要添加额外的字段
2. 确保JSON格式正确，没有语法错误
3. 如果某个字段没有对应信息，填空字符串""
4. 数组类型字段如果没有相关信息则返回空数组[]
5. 质量检查规则和关联案例都需要尽可能完整抽取

### 待解析文档内容：
{{ document_content }}
